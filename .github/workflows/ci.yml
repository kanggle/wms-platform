name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  # ---------------------------------------------------------------------------
  # changes — single source of truth for path-based job filtering.
  #
  # dorny/paths-filter@v3 computes which project areas were touched in this
  # PR / push. Every downstream job reads these outputs via `needs.changes`
  # and applies:
  #
  #   if: needs.changes.outputs.libs == 'true'
  #       || needs.changes.outputs.workflows == 'true'
  #       || needs.changes.outputs.<project> == 'true'
  #
  # Filter rules (path → flag):
  #   libs       libs/ platform/ rules/ .claude/ build.gradle settings.gradle
  #              package.json tsconfig.base.json tasks/templates/
  #              → activates ALL downstream jobs (cross-project impact)
  #   workflows  .github/workflows/
  #              → activates ALL downstream jobs (CI self-change safety)
  #   wms        **
  #   ecommerce  projects/ecommerce-microservices-platform/**
  #   gap        projects/global-account-platform/**
  #   fan        projects/fan-platform/**
  #   scm        projects/scm-platform/**
  #
  # Edge case — workflow self-change PR (.github/workflows/** modified):
  #   The `workflows` flag catches it → all jobs activate (regression safety).
  #
  # Edge case — dorny/paths-filter step itself fails (bug/timeout):
  #   All outputs default to empty string (falsy).  The `if:` conditions below
  #   use `|| github.event_name == 'push'` as a push-to-main fallback so main
  #   branch protection is never compromised by a filter failure.
  #   PR-level filtering simply falls back to "all skip" — the author can
  #   close+reopen the PR to retry or add an empty commit to re-trigger.
  #
  # Edge case — tasks/** or *.md only change:
  #   No project flag is true; libs/workflows are also false.
  #   build-and-test activates via its own fallback (it is always-on for pushes
  #   to main); all other jobs skip. Spec/chore PRs complete in ~1-2 min.
  #   TASK-MONO-045.
  # ---------------------------------------------------------------------------
  changes:
    runs-on: ubuntu-latest
    outputs:
      libs: ${{ steps.filter.outputs.libs }}
      workflows: ${{ steps.filter.outputs.workflows }}
      wms: ${{ steps.filter.outputs.wms }}
      ecommerce: ${{ steps.filter.outputs.ecommerce }}
      gap: ${{ steps.filter.outputs.gap }}
      fan: ${{ steps.filter.outputs.fan }}
      scm: ${{ steps.filter.outputs.scm }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            libs:
              - 'libs/**'
              - 'platform/**'
              - 'rules/**'
              - '.claude/**'
              - 'build.gradle'
              - 'settings.gradle'
              - 'package.json'
              - 'tsconfig.base.json'
              - 'tasks/templates/**'
            workflows:
              - '.github/workflows/**'
            wms:
              - '**'
            ecommerce:
              - 'projects/ecommerce-microservices-platform/**'
            gap:
              - 'projects/global-account-platform/**'
            fan:
              - 'projects/fan-platform/**'
            scm:
              - 'projects/scm-platform/**'

  build-and-test:
    name: Build & Test (JDK 21, Linux)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: changes
    # Activate when:
    #   - any shared library / cross-project path changed (libs, workflows)
    #   - any project's backend code changed (wms, ecommerce, gap, fan, scm)
    #   - push to main always runs (safety net regardless of filter)
    # spec/chore PRs (tasks/** or *.md only) produce no matching flag → skip.
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.wms == 'true' ||
      needs.changes.outputs.ecommerce == 'true' ||
      needs.changes.outputs.gap == 'true' ||
      needs.changes.outputs.fan == 'true' ||
      needs.changes.outputs.scm == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Build and test (libs + wms-platform + ecommerce backend)
        # Scope rationale:
        # - libs/* and wms-platform/* always run (the wms baseline).
        # - ecommerce-microservices-platform/* run on a per-service basis after
        #   TASK-MONO-004 added @Tag("integration") to Testcontainers-backed
        #   classes and made the default :test task Docker-free.
        # - 11 ecommerce backend services run (auth-service removed by TASK-BE-132).
        # Use :check (not :test) on every entry — matches the wms style, and
        # picks up jacoco / lint where defined.
        run: >-
          ./gradlew
          :libs:java-common:check
          :libs:java-messaging:check
          :libs:java-observability:check
          :libs:java-security:check
          :libs:java-test-support:check
          :libs:java-web:check
          :apps:gateway-service:check
          :apps:inbound-service:check
          :apps:inventory-service:check
          :apps:outbound-service:check
          :apps:master-service:check
          :apps:notification-service:check
          :apps:admin-service:check
          :projects:ecommerce-microservices-platform:apps:batch-worker:check
          :projects:ecommerce-microservices-platform:apps:gateway-service:check
          :projects:ecommerce-microservices-platform:apps:notification-service:check
          :projects:ecommerce-microservices-platform:apps:order-service:check
          :projects:ecommerce-microservices-platform:apps:payment-service:check
          :projects:ecommerce-microservices-platform:apps:product-service:check
          :projects:ecommerce-microservices-platform:apps:promotion-service:check
          :projects:ecommerce-microservices-platform:apps:review-service:check
          :projects:ecommerce-microservices-platform:apps:search-service:check
          :projects:ecommerce-microservices-platform:apps:shipping-service:check
          :projects:ecommerce-microservices-platform:apps:user-service:check
          --no-daemon --stacktrace

      - name: Build and check GAP backend (Docker-free)
        # GAP integration tests carry @Tag("integration") (either directly or
        # via AbstractIntegrationTest base) and are excluded from `:test` /
        # `:check` by `projects/global-account-platform/build.gradle`. They
        # run in the dedicated `gap-integration-tests` job below. This step
        # therefore stays Docker-free.
        run: >-
          ./gradlew
          :projects:global-account-platform:apps:auth-service:check
          :projects:global-account-platform:apps:account-service:check
          :projects:global-account-platform:apps:admin-service:check
          :projects:global-account-platform:apps:community-service:check
          :projects:global-account-platform:apps:gateway-service:check
          :projects:global-account-platform:apps:membership-service:check
          :projects:global-account-platform:apps:security-service:check
          --no-daemon --stacktrace

      - name: Build and check fan-platform backend (Docker-free)
        # fan-platform integration tests carry @Tag("integration"); the e2e
        # module's tests carry @Tag("e2e"). Both are excluded from default
        # :test/:check tasks (see each module's build.gradle). The e2e job
        # below runs them with Docker.
        run: >-
          ./gradlew
          :projects:fan-platform:apps:gateway-service:check
          :projects:fan-platform:apps:community-service:check
          :projects:fan-platform:apps:artist-service:check
          :projects:fan-platform:tests:e2e:check
          --no-daemon --stacktrace

      - name: Build and check scm-platform backend (Docker-free)
        # scm-platform integration tests carry @Tag("integration") and are
        # excluded from default :test/:check tasks (see each module's
        # build.gradle). Docker-backed integrationTest is invoked separately
        # on dev environments / scm-integration-tests job below.
        # TASK-SCM-BE-001: gateway-service
        # TASK-SCM-BE-002: procurement-service
        # TASK-SCM-BE-003: inventory-visibility-service
        run: >-
          ./gradlew
          :projects:scm-platform:apps:gateway-service:check
          :projects:scm-platform:apps:procurement-service:check
          :projects:scm-platform:apps:inventory-visibility-service:check
          --no-daemon --stacktrace

      - name: Upload test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            **/build/reports/tests/
            **/build/test-results/
          retention-days: 7

  boot-jars:
    name: Package boot jars
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 15
    # Activate for wms changes or cross-project (libs/workflows).
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.wms == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build boot jars
        run: >-
          ./gradlew
          :apps:master-service:bootJar
          :apps:gateway-service:bootJar
          :apps:notification-service:bootJar
          :apps:admin-service:bootJar
          --no-daemon

      - name: Upload jars
        uses: actions/upload-artifact@v4
        with:
          name: wms-boot-jars
          path: |
            apps/master-service/build/libs/master-service.jar
            apps/gateway-service/build/libs/gateway-service.jar
            apps/notification-service/build/libs/notification-service.jar
            apps/admin-service/build/libs/admin-service.jar
          retention-days: 7

  ecommerce-boot-jars:
    name: Package boot jars (ecommerce)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 15
    # Activate for ecommerce changes or cross-project (libs/workflows).
    # Packages all 12 ecommerce backend services and uploads the JARs as an
    # artifact consumed by the nightly-e2e workflow (TASK-MONO-014, TASK-MONO-045).
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.ecommerce == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build ecommerce boot jars
        # auth-service excluded by TASK-BE-132 — decommissioned, not in settings.gradle.
        run: >-
          ./gradlew
          :projects:ecommerce-microservices-platform:apps:batch-worker:bootJar
          :projects:ecommerce-microservices-platform:apps:gateway-service:bootJar
          :projects:ecommerce-microservices-platform:apps:notification-service:bootJar
          :projects:ecommerce-microservices-platform:apps:order-service:bootJar
          :projects:ecommerce-microservices-platform:apps:payment-service:bootJar
          :projects:ecommerce-microservices-platform:apps:product-service:bootJar
          :projects:ecommerce-microservices-platform:apps:promotion-service:bootJar
          :projects:ecommerce-microservices-platform:apps:review-service:bootJar
          :projects:ecommerce-microservices-platform:apps:search-service:bootJar
          :projects:ecommerce-microservices-platform:apps:shipping-service:bootJar
          :projects:ecommerce-microservices-platform:apps:user-service:bootJar
          --no-daemon --stacktrace

      - name: Upload ecommerce boot jars
        uses: actions/upload-artifact@v4
        with:
          name: ecommerce-boot-jars
          # upload-artifact@v4 strips the longest common prefix from multi-path
          # uploads. All 11 paths share `projects/ecommerce-microservices-platform/apps/`
          # so the artifact root contains `<service>/build/libs/<service>.jar`.
          # The frontend-e2e job restores them to their canonical locations.
          # auth-service excluded by TASK-BE-132 — decommissioned.
          path: |
            projects/ecommerce-microservices-platform/apps/batch-worker/build/libs/batch-worker.jar
            projects/ecommerce-microservices-platform/apps/gateway-service/build/libs/gateway-service.jar
            projects/ecommerce-microservices-platform/apps/notification-service/build/libs/notification-service.jar
            projects/ecommerce-microservices-platform/apps/order-service/build/libs/order-service.jar
            projects/ecommerce-microservices-platform/apps/payment-service/build/libs/payment-service.jar
            projects/ecommerce-microservices-platform/apps/product-service/build/libs/product-service.jar
            projects/ecommerce-microservices-platform/apps/promotion-service/build/libs/promotion-service.jar
            projects/ecommerce-microservices-platform/apps/review-service/build/libs/review-service.jar
            projects/ecommerce-microservices-platform/apps/search-service/build/libs/search-service.jar
            projects/ecommerce-microservices-platform/apps/shipping-service/build/libs/shipping-service.jar
            projects/ecommerce-microservices-platform/apps/user-service/build/libs/user-service.jar
          retention-days: 1

  fan-platform-boot-jars:
    name: Package boot jars (fan-platform)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 15
    # Activate for fan-platform changes or cross-project (libs/workflows).
    # Packages the 3 fan-platform v1 backend services (gateway / community /
    # artist) as boot jars and uploads them as a single artifact consumed by
    # the `fan-platform-e2e` job (TASK-FAN-INT-001).
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.fan == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build fan-platform boot jars
        run: >-
          ./gradlew
          :projects:fan-platform:apps:gateway-service:bootJar
          :projects:fan-platform:apps:community-service:bootJar
          :projects:fan-platform:apps:artist-service:bootJar
          --no-daemon --stacktrace

      - name: Upload fan-platform boot jars
        uses: actions/upload-artifact@v4
        with:
          name: fan-platform-boot-jars
          # upload-artifact@v4 strips the longest common prefix
          # (`projects/fan-platform/apps/`) so the artifact root contains
          # `<service>/build/libs/<service>.jar`. The fan-platform-e2e job
          # restores them to their canonical locations.
          path: |
            projects/fan-platform/apps/gateway-service/build/libs/gateway-service.jar
            projects/fan-platform/apps/community-service/build/libs/community-service.jar
            projects/fan-platform/apps/artist-service/build/libs/artist-service.jar
          retention-days: 1

  scm-platform-boot-jars:
    name: Package boot jars (scm-platform)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 15
    # Activate for scm-platform changes or cross-project (libs/workflows).
    # Packages the 3 scm-platform v1 backend services (gateway / procurement /
    # inventory-visibility) as boot jars and uploads them as a single artifact
    # consumed by the `scm-platform-e2e` job (TASK-SCM-INT-001).
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.scm == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build scm-platform boot jars
        run: >-
          ./gradlew
          :projects:scm-platform:apps:gateway-service:bootJar
          :projects:scm-platform:apps:procurement-service:bootJar
          :projects:scm-platform:apps:inventory-visibility-service:bootJar
          --no-daemon --stacktrace

      - name: Upload scm-platform boot jars
        uses: actions/upload-artifact@v4
        with:
          name: scm-platform-boot-jars
          # upload-artifact@v4 strips the longest common prefix
          # (`projects/scm-platform/apps/`) so the artifact root contains
          # `<service>/build/libs/<service>.jar`. The scm-platform-e2e job
          # restores them to their canonical locations.
          path: |
            projects/scm-platform/apps/gateway-service/build/libs/gateway-service.jar
            projects/scm-platform/apps/procurement-service/build/libs/procurement-service.jar
            projects/scm-platform/apps/inventory-visibility-service/build/libs/inventory-visibility-service.jar
          retention-days: 1

  frontend-unit-tests:
    name: Frontend unit tests (ecommerce + fan-platform, vitest)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: changes
    # Activate for ecommerce or fan-platform frontend changes, or cross-project.
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.ecommerce == 'true' ||
      needs.changes.outputs.fan == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '9.15.0'

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: |
            projects/ecommerce-microservices-platform/pnpm-lock.yaml
            projects/fan-platform/pnpm-lock.yaml

      - name: Install dependencies (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm install --frozen-lockfile

      - name: Run unit tests (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm test

      - name: Install dependencies (fan-platform)
        working-directory: projects/fan-platform
        # No lockfile checked in yet — TASK-FAN-FE-001 only adds the workspace
        # skeleton. Allow non-frozen install to bootstrap the lockfile.
        run: pnpm install --no-frozen-lockfile

      - name: Run unit tests (fan-platform)
        working-directory: projects/fan-platform
        run: pnpm test

  frontend-e2e-smoke:
    name: Frontend E2E smoke (web-store + fan-platform-web, Playwright)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: changes
    # Frontend CI Phase 3 (TASK-MONO-013) extended for fan-platform-web
    # (TASK-FAN-FE-001).
    # Smoke-level E2E only — both apps' playwright.smoke.config.ts force SSR
    # fetch / OIDC discovery to a closed loopback host so each page exercises
    # its fallback path / middleware redirect. No backend stack needed; runs
    # in extracted portfolio repos as well as monorepo-lab.
    # Activate for ecommerce or fan-platform frontend changes, or cross-project.
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.ecommerce == 'true' ||
      needs.changes.outputs.fan == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '9.15.0'

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: |
            projects/ecommerce-microservices-platform/pnpm-lock.yaml
            projects/fan-platform/pnpm-lock.yaml

      - name: Install dependencies (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers (chromium, with system deps)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm --filter web-store exec playwright install --with-deps chromium

      - name: Run smoke E2E (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm e2e:smoke

      - name: Install dependencies (fan-platform)
        working-directory: projects/fan-platform
        run: pnpm install --no-frozen-lockfile

      - name: Build fan-platform-web (Next.js prod build for smoke)
        working-directory: projects/fan-platform
        env:
          # Smoke uses the same closed-loopback URL forced by playwright.smoke.config.ts
          NEXT_PUBLIC_GATEWAY_URL: http://127.0.0.1:1
        run: pnpm --filter fan-platform-web build

      - name: Install Playwright browsers (fan-platform-web)
        working-directory: projects/fan-platform
        run: pnpm --filter fan-platform-web exec playwright install --with-deps chromium

      - name: Run smoke E2E (fan-platform-web)
        working-directory: projects/fan-platform
        run: pnpm e2e:smoke

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-smoke
          path: |
            projects/ecommerce-microservices-platform/apps/web-store/playwright-report-smoke/
            projects/ecommerce-microservices-platform/apps/web-store/test-results-smoke/
            projects/fan-platform/web/fan-platform-web/playwright-report-smoke/
            projects/fan-platform/web/fan-platform-web/test-results-smoke/
          retention-days: 7

  integration-tests:
    name: Integration (master-service + notification-service, Testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 20
    # Integration suite boots Postgres + Kafka + Redis via Testcontainers.
    # Mirrors the e2e job's gating: runs only on the monorepo-lab dev
    # environment, skipped in the portfolio extraction (kanggle/wms-platform)
    # whose badge reflects the Docker-free `check` signal. TASK-BE-017.
    # Activate for wms changes or cross-project (libs/workflows).
    if: >-
      github.repository == 'kanggle/monorepo-lab' &&
      (
        github.event_name == 'push' ||
        needs.changes.outputs.libs == 'true' ||
        needs.changes.outputs.workflows == 'true' ||
        needs.changes.outputs.wms == 'true'
      )

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Run wms integration suites
        # TASK-BE-043: notification-service joins the wms integration job.
        # TASK-BE-045: admin-service joins. All three services boot Postgres
        # (+ Kafka where applicable) via Testcontainers; sharing the job
        # avoids spinning up additional runners.
        run: >-
          ./gradlew
          :apps:master-service:integrationTest
          :apps:notification-service:integrationTest
          :apps:admin-service:integrationTest
          --no-daemon --stacktrace

      - name: Upload integration test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-reports
          path: |
            apps/master-service/build/reports/tests/integrationTest/
            apps/master-service/build/test-results/integrationTest/
            apps/notification-service/build/reports/tests/integrationTest/
            apps/notification-service/build/test-results/integrationTest/
            apps/admin-service/build/reports/tests/integrationTest/
            apps/admin-service/build/test-results/integrationTest/
          retention-days: 7

  gap-integration-tests:
    name: Integration (global-account-platform, Testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 30
    # GAP integration suite boots MySQL + Kafka + (per-service Redis/WireMock)
    # via Testcontainers. Scoped to monorepo-lab; the standalone repo
    # extraction relies on its own Docker-free `check` signal.
    # Activate for GAP changes or cross-project (libs/workflows).
    if: >-
      github.repository == 'kanggle/monorepo-lab' &&
      (
        github.event_name == 'push' ||
        needs.changes.outputs.libs == 'true' ||
        needs.changes.outputs.workflows == 'true' ||
        needs.changes.outputs.gap == 'true'
      )

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Run GAP integration suites
        run: >-
          ./gradlew
          :projects:global-account-platform:apps:auth-service:integrationTest
          :projects:global-account-platform:apps:account-service:integrationTest
          :projects:global-account-platform:apps:admin-service:integrationTest
          :projects:global-account-platform:apps:community-service:integrationTest
          :projects:global-account-platform:apps:gateway-service:integrationTest
          :projects:global-account-platform:apps:membership-service:integrationTest
          :projects:global-account-platform:apps:security-service:integrationTest
          --no-daemon --stacktrace

      - name: Upload GAP integration test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gap-integration-test-reports
          path: |
            projects/global-account-platform/apps/*/build/reports/tests/integrationTest/
            projects/global-account-platform/apps/*/build/test-results/integrationTest/
          retention-days: 7

  scm-integration-tests:
    name: Integration (scm-platform, Testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test]
    timeout-minutes: 30
    # SCM integration suite boots MySQL + Postgres + (per-service WireMock)
    # via Testcontainers. Scoped to monorepo-lab; standalone repo extraction
    # relies on its own Docker-free `check` signal.
    # Activate for SCM changes or cross-project (libs/workflows). TASK-MONO-048.
    if: >-
      github.repository == 'kanggle/monorepo-lab' &&
      (
        github.event_name == 'push' ||
        needs.changes.outputs.libs == 'true' ||
        needs.changes.outputs.workflows == 'true' ||
        needs.changes.outputs.scm == 'true'
      )

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Run SCM integration suites
        run: >-
          ./gradlew
          :projects:scm-platform:apps:procurement-service:integrationTest
          :projects:scm-platform:apps:inventory-visibility-service:integrationTest
          --no-daemon --stacktrace

      - name: Upload SCM integration test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scm-integration-test-reports
          path: |
            projects/scm-platform/apps/*/build/reports/tests/integrationTest/
            projects/scm-platform/apps/*/build/test-results/integrationTest/
          retention-days: 7

  frontend-checks:
    name: Frontend lint & build (ecommerce + fan-platform)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: changes
    # Activate for ecommerce or fan-platform frontend changes, or cross-project.
    # Frontend build depends on shared tsconfig.base.json (captured by libs flag).
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.libs == 'true' ||
      needs.changes.outputs.workflows == 'true' ||
      needs.changes.outputs.ecommerce == 'true' ||
      needs.changes.outputs.fan == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '9.15.0'

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: |
            projects/ecommerce-microservices-platform/pnpm-lock.yaml
            projects/fan-platform/pnpm-lock.yaml

      - name: Install dependencies (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm install --frozen-lockfile

      - name: Lint (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm lint

      - name: Build (ecommerce)
        working-directory: projects/ecommerce-microservices-platform
        run: pnpm build

      - name: Install dependencies (fan-platform)
        working-directory: projects/fan-platform
        run: pnpm install --no-frozen-lockfile

      - name: Lint (fan-platform)
        working-directory: projects/fan-platform
        run: pnpm lint

      - name: Build (fan-platform)
        working-directory: projects/fan-platform
        env:
          NEXT_PUBLIC_GATEWAY_URL: http://fan-platform.local
        run: pnpm build

  # frontend-e2e (full-stack web-store) has been moved to nightly-e2e.yml.
  # It runs on schedule cron '0 18 * * *' (UTC 18:00 = KST 03:00) and is
  # triggered manually via workflow_dispatch. TASK-MONO-045.

  e2e-tests:
    name: E2E (gateway-master live-pair, Testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test, boot-jars]
    timeout-minutes: 60
    # Scoped to the monorepo-lab dev environment (see integration-tests comment).
    # Activate for wms changes or cross-project (libs/workflows).
    if: >-
      github.repository == 'kanggle/monorepo-lab' &&
      (
        github.event_name == 'push' ||
        needs.changes.outputs.libs == 'true' ||
        needs.changes.outputs.workflows == 'true' ||
        needs.changes.outputs.wms == 'true'
      )

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Download boot jars
        uses: actions/download-artifact@v4
        with:
          name: wms-boot-jars
          path: artifact-staging

      - name: Restore boot jar paths
        # actions/upload-artifact@v4 strips the longest common prefix when
        # multiple paths are uploaded, so the wms-boot-jars artifact contains
        # `master-service/build/libs/master-service.jar` and
        # `gateway-service/build/libs/gateway-service.jar` (with the
        # `apps/` prefix removed). The e2e harness's
        # locateFile() in E2EBase.java expects the original
        # `apps/<service>/build/libs/<service>.jar` layout when resolving
        # paths from gateway-service's project directory, so move the
        # jars back to their canonical locations before invoking Gradle.
        run: |
          mkdir -p apps/master-service/build/libs/
          mkdir -p apps/gateway-service/build/libs/
          mv artifact-staging/master-service/build/libs/master-service.jar \
             apps/master-service/build/libs/master-service.jar
          mv artifact-staging/gateway-service/build/libs/gateway-service.jar \
             apps/gateway-service/build/libs/gateway-service.jar

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Build service images for e2e
        # Use the Docker CLI (BuildKit) instead of Testcontainers' ImageFromDockerfile
        # (Docker Java client REST API). On Docker 28 the REST /build endpoint routes
        # through BuildKit's gRPC session mechanism and hangs indefinitely with the
        # Docker Java client bundled in Testcontainers 1.21.3 — 58-min and 60-min
        # diagnostic runs confirmed zero progress after the image-check log line.
        # Pre-building here and passing the image names via -Dwms.e2e.* lets
        # E2EBase.java skip ImageFromDockerfile and use GenericContainer directly.
        run: |
          docker build -t wms-master-service:e2e \
            -f apps/master-service/Dockerfile \
            apps/master-service/
          docker build -t wms-gateway-service:e2e \
            -f apps/gateway-service/Dockerfile \
            apps/gateway-service/

      - name: Run gateway-master e2e suite
        run: >-
          ./gradlew
          :apps:gateway-service:e2eTest
          -x :apps:master-service:bootJar
          -x :apps:gateway-service:bootJar
          -Dwms.e2e.masterImage=wms-master-service:e2e
          -Dwms.e2e.gatewayImage=wms-gateway-service:e2e
          --no-daemon --stacktrace

      - name: Upload e2e test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-reports
          path: |
            apps/gateway-service/build/reports/tests/e2eTest/
            apps/gateway-service/build/test-results/e2eTest/
          retention-days: 7

  fan-platform-e2e:
    name: E2E (fan-platform v1 live-trio, Testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test, fan-platform-boot-jars]
    timeout-minutes: 20
    # Live-trio e2e suite (TASK-FAN-INT-001): boots gateway-service +
    # community-service + artist-service plus Postgres + Redis + Kafka
    # via Testcontainers and a host-side WireMock JWKS stand-in. Mirrors
    # the wms `e2e-tests` job's pattern: pre-build images via the Docker
    # CLI to dodge the Docker 28 BuildKit gRPC hang in
    # `ImageFromDockerfile`, then pass the image names via -Dfan.e2e.*
    # so FanPlatformE2ETestBase uses GenericContainer directly.
    # Activate for fan-platform changes or cross-project (libs/workflows).
    if: >-
      github.repository == 'kanggle/monorepo-lab' &&
      (
        github.event_name == 'push' ||
        needs.changes.outputs.libs == 'true' ||
        needs.changes.outputs.workflows == 'true' ||
        needs.changes.outputs.fan == 'true'
      )

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Download fan-platform boot jars
        uses: actions/download-artifact@v4
        with:
          name: fan-platform-boot-jars
          path: artifact-staging-fan

      - name: Restore fan-platform boot jar paths
        # actions/upload-artifact@v4 strips the longest common prefix when
        # multiple paths are uploaded. The fan-platform-boot-jars artifact
        # therefore contains `<service>/build/libs/<service>.jar` (with
        # `projects/fan-platform/apps/` stripped). Move them back to their
        # canonical locations so the gateway/community/artist Dockerfiles
        # COPY the right path during the docker build below, AND so
        # FanPlatformE2ETestBase's locateOptionalJar fallback can resolve
        # them when the dev-path system properties are unset.
        run: |
          mkdir -p projects/fan-platform/apps/gateway-service/build/libs/
          mkdir -p projects/fan-platform/apps/community-service/build/libs/
          mkdir -p projects/fan-platform/apps/artist-service/build/libs/
          mv artifact-staging-fan/gateway-service/build/libs/gateway-service.jar \
             projects/fan-platform/apps/gateway-service/build/libs/gateway-service.jar
          mv artifact-staging-fan/community-service/build/libs/community-service.jar \
             projects/fan-platform/apps/community-service/build/libs/community-service.jar
          mv artifact-staging-fan/artist-service/build/libs/artist-service.jar \
             projects/fan-platform/apps/artist-service/build/libs/artist-service.jar

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Build fan-platform service images for e2e
        # Docker CLI build (BuildKit) — see wms `e2e-tests` job for the
        # rationale (Docker 28 + Testcontainers ImageFromDockerfile hang).
        run: |
          docker build -t fan-platform-gateway-service:e2e \
            -f projects/fan-platform/apps/gateway-service/Dockerfile \
            projects/fan-platform/apps/gateway-service/
          docker build -t fan-platform-community-service:e2e \
            -f projects/fan-platform/apps/community-service/Dockerfile \
            projects/fan-platform/apps/community-service/
          docker build -t fan-platform-artist-service:e2e \
            -f projects/fan-platform/apps/artist-service/Dockerfile \
            projects/fan-platform/apps/artist-service/

      - name: Run fan-platform live-trio e2e suite
        run: >-
          ./gradlew
          :projects:fan-platform:tests:e2e:e2eTest
          -x :projects:fan-platform:apps:gateway-service:bootJar
          -x :projects:fan-platform:apps:community-service:bootJar
          -x :projects:fan-platform:apps:artist-service:bootJar
          -Dfan.e2e.gatewayImage=fan-platform-gateway-service:e2e
          -Dfan.e2e.communityImage=fan-platform-community-service:e2e
          -Dfan.e2e.artistImage=fan-platform-artist-service:e2e
          --no-daemon --stacktrace

      - name: Upload fan-platform e2e test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fan-platform-e2e-test-reports
          path: |
            projects/fan-platform/tests/e2e/build/reports/tests/e2eTest/
            projects/fan-platform/tests/e2e/build/test-results/e2eTest/
          retention-days: 7

  scm-platform-e2e:
    name: E2E (scm-platform v1 cross-service, Testcontainers)
    runs-on: ubuntu-latest
    needs: [changes, build-and-test, scm-platform-boot-jars]
    timeout-minutes: 25
    # Cross-service e2e suite (TASK-SCM-INT-001): boots gateway-service +
    # procurement-service + inventory-visibility-service plus Postgres +
    # Redis + Kafka via Testcontainers, a host-side WireMock JWKS stand-in,
    # and a host-side supplier MockWebServer. Mirrors the fan-platform-e2e
    # job's pattern: pre-build images via the Docker CLI to dodge the
    # Docker 28 BuildKit gRPC hang in `ImageFromDockerfile`, then pass the
    # image names via -Dscm.e2e.* so ScmPlatformE2ETestBase uses
    # GenericContainer directly.
    # Activate for scm-platform changes or cross-project (libs/workflows).
    if: >-
      github.repository == 'kanggle/monorepo-lab' &&
      (
        github.event_name == 'push' ||
        needs.changes.outputs.libs == 'true' ||
        needs.changes.outputs.workflows == 'true' ||
        needs.changes.outputs.scm == 'true'
      )

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Download scm-platform boot jars
        uses: actions/download-artifact@v4
        with:
          name: scm-platform-boot-jars
          path: artifact-staging-scm

      - name: Restore scm-platform boot jar paths
        # actions/upload-artifact@v4 strips the longest common prefix when
        # multiple paths are uploaded. The scm-platform-boot-jars artifact
        # therefore contains `<service>/build/libs/<service>.jar` (with
        # `projects/scm-platform/apps/` stripped). Move them back to their
        # canonical locations so the gateway/procurement/inventory-visibility
        # Dockerfiles COPY the right path during the docker build below, AND
        # so ScmPlatformE2ETestBase's locateOptionalJar fallback can resolve
        # them when the dev-path system properties are unset.
        run: |
          mkdir -p projects/scm-platform/apps/gateway-service/build/libs/
          mkdir -p projects/scm-platform/apps/procurement-service/build/libs/
          mkdir -p projects/scm-platform/apps/inventory-visibility-service/build/libs/
          mv artifact-staging-scm/gateway-service/build/libs/gateway-service.jar \
             projects/scm-platform/apps/gateway-service/build/libs/gateway-service.jar
          mv artifact-staging-scm/procurement-service/build/libs/procurement-service.jar \
             projects/scm-platform/apps/procurement-service/build/libs/procurement-service.jar
          mv artifact-staging-scm/inventory-visibility-service/build/libs/inventory-visibility-service.jar \
             projects/scm-platform/apps/inventory-visibility-service/build/libs/inventory-visibility-service.jar

      - name: Verify Docker (Testcontainers prerequisite)
        run: docker info

      - name: Build scm-platform service images for e2e
        # Docker CLI build (BuildKit) — see wms `e2e-tests` job for the
        # rationale (Docker 28 + Testcontainers ImageFromDockerfile hang).
        run: |
          docker build -t scm-platform-gateway-service:e2e \
            -f projects/scm-platform/apps/gateway-service/Dockerfile \
            projects/scm-platform/apps/gateway-service/
          docker build -t scm-platform-procurement-service:e2e \
            -f projects/scm-platform/apps/procurement-service/Dockerfile \
            projects/scm-platform/apps/procurement-service/
          docker build -t scm-platform-inventory-visibility-service:e2e \
            -f projects/scm-platform/apps/inventory-visibility-service/Dockerfile \
            projects/scm-platform/apps/inventory-visibility-service/

      - name: Run scm-platform cross-service e2e suite
        run: >-
          ./gradlew
          :projects:scm-platform:tests:e2e:e2eTest
          -x :projects:scm-platform:apps:gateway-service:bootJar
          -x :projects:scm-platform:apps:procurement-service:bootJar
          -x :projects:scm-platform:apps:inventory-visibility-service:bootJar
          -Dscm.e2e.gatewayImage=scm-platform-gateway-service:e2e
          -Dscm.e2e.procurementImage=scm-platform-procurement-service:e2e
          -Dscm.e2e.inventoryVisibilityImage=scm-platform-inventory-visibility-service:e2e
          --no-daemon --stacktrace

      - name: Upload scm-platform e2e test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scm-platform-e2e-test-reports
          path: |
            projects/scm-platform/tests/e2e/build/reports/tests/e2eTest/
            projects/scm-platform/tests/e2e/build/test-results/e2eTest/
          retention-days: 7
