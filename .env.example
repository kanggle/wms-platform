# =============================================================================
# WMS Platform — Environment Variables
# =============================================================================
# Copy this file to .env and adjust values as needed.
# Defaults are tuned so `docker compose up -d` works without .env.
#
# ⚠️  Values with the `-changeme-local` suffix are local-dev-only placeholders.
#     Replace them with unique, strong values before any non-local deploy.
# =============================================================================

# --- Hostname routing (per ADR-MONO-001 Option C, TASK-MONO-024) ---
# When wms application services run as containers (full-stack scenarios),
# gateway-service is reached via http://wms.local/ — Traefik routes by Host
# header. Backing services do not publish host ports.
#
# Prerequisites (one-time):
#   bash scripts/dev-setup.sh                      # *.local hosts registration
#   pnpm traefik:up                                # shared Traefik stack
#
# bootRun workflow (default for local dev): use the bootrun overlay so
# Postgres/Kafka/Redis are reachable on host ports 25432/29093/26379:
#   pnpm wms:bootrun
PROJECT_HOSTNAME=wms.local

# --- Postgres ---
POSTGRES_ROOT_PASSWORD=postgres-changeme-local

# Service-user passwords (one per wms service that owns state)
MASTER_DB_PASSWORD=master-changeme-local
INBOUND_DB_PASSWORD=inbound-changeme-local
INVENTORY_DB_PASSWORD=inventory-changeme-local
OUTBOUND_DB_PASSWORD=outbound-changeme-local
ADMIN_DB_PASSWORD=admin-changeme-local
NOTIFICATION_DB_PASSWORD=notification-changeme-local

# --- Kafka (KRaft) ---
KAFKA_KRAFT_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qg

# --- Slack incoming webhooks (notification-service, TASK-BE-043) ---
# Inject these per channel alias. Empty values cause SlackChannelAdapter to
# raise ChannelNotConfiguredException → delivery FAILED + outbox audit. This
# is intentional: dev / CI fail-closed contract (architecture.md § Edge Cases).
# Format: https://hooks.slack.com/services/T<team>/B<bot>/<token>
SLACK_WEBHOOK_URL_WMS_ALERTS=
SLACK_WEBHOOK_URL_WMS_SHIPPING=

# --- Observability ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin-changeme-local
